name: Deploy-App-to-EKS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:

    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    - name: Login to ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v2

    - name: Build Docker Image
      run: |
        docker build -t eks-demo-app .

    - name: Tag Docker Image
      run: |
        docker tag eks-demo-app:latest ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/eks-demo-app:latest

    - name: Push Docker Image
      run: |
        docker push ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/eks-demo-app:latest

    - name: Update kubeconfig
      run: |
        aws eks update-kubeconfig \
        --region us-east-1 \
        --name vinod-eks-cluster

    - name: Deploy to Kubernetes
      run: |
        kubectl apply -f k8s/